{
  "name": "keyboard-shortcuts-extension",
  "version": "2.0.1",
  "scripts": {
    "build": "esbuild extension/src/app/index.js --bundle --outfile=dist/bundle.js --format=iife",
    "release": "npm run build && npm run copy-release",
    "copy-release": "node scripts/make-release.js"
  },
  "devDependencies": {
    "esbuild": "^0.19.0",
    "archiver": "^6.0.0"
  }
}